#pragma once

// Returns the square of value. Deliberately trivial and warning-clean so the example
// builds under a strict warning level (/W4 or -Wall -Wextra -Wpedantic) on every compiler.
int squared(int value);
