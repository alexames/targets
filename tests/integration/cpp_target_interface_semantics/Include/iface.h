// Header for the header-only INTERFACE library exercised by the issue #13 regression
// test. Its contents are irrelevant; its mere presence makes cpp_library resolve the
// target to an INTERFACE (header-only) library.
#pragma once
