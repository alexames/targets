// Stub translation unit for the multi-directory cpp_test regression test (issue #62).
// Never compiled -- the test only configures and generates. A real cpp_test binary gets
// its entry point from the linked GTest::gtest_main.
static int targets_issue62_first() { return 0; }
